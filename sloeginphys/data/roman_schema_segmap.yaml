%YAML 1.1
---
$schema: "http://stsci.edu/schemas/yaml-schema/draft-01"

title: Internal Roman SN PIT schema
description: |
  An asdf file with data, error, WCS, and other metadata required for running the Roman SN PIT fitting code

type: object


properties:
  roman:
    title: Roman
    properties:
      data: 
        title: data
      meta:
        title: meta
        properties:
          wcs: 
            title: wcs
        required: [wcs]
    required: [data]
required: [roman]
additionalProperties: true
...